[pytest]
markers =
    smoke: Critical tests for a quick sanity check.
    api: Tests for backend endpoints and responses.
    integration: Tests checking how different modules work together.
    ui: Standard browser-based tests.